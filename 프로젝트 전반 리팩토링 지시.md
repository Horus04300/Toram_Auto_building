# 프로젝트 전반 리팩토링 지시

현재 프로젝트의 기능을 유지하면서 코드베이스 전반을 리팩토링한다.

## 1. 최우선 원칙

리팩토링의 목적은 새로운 기능 추가가 아니라 다음을 개선하는 것이다.

- 코드 가독성
- 모듈 간 책임 분리
- 중복 코드 제거
- 데이터 흐름 단순화
- 유지보수성
- 테스트 용이성
- 향후 기능 확장성

기존에 정상 동작하던 기능과 계산 결과는 원칙적으로 변경하지 않는다.

즉,

> **동작은 유지하고 구조를 개선한다.**

---

## 2. 작업 전 분석

코드를 즉시 수정하지 말고 먼저 현재 프로젝트 구조를 분석한다.

다음을 파악한다.

```text
Project
├─ UI Layer
│  ├─ 화면 렌더링
│  ├─ 사용자 입력
│  └─ 상태 표시
│
├─ Application / Controller Layer
│  ├─ 사용자 동작 처리
│  ├─ 계산 요청
│  └─ 상태 전환
│
├─ Domain / Calculation Layer
│  ├─ 스탯 계산
│  ├─ 장비/스킬 효과
│  ├─ 조건부 효과
│  └─ 빌드 계산 로직
│
├─ Data Layer
│  ├─ 장비 데이터
│  ├─ 스킬 데이터
│  ├─ 크리스타 데이터
│  └─ 설정/저장 데이터
│
└─ Test
   ├─ 단위 테스트
   ├─ 회귀 테스트
   └─ 통합 테스트
```

실제 코드가 이 구조와 다르다면 현재 구조를 먼저 설명하고, 각 모듈의 책임과 의존 관계를 정리한다.

특히 다음 문제를 탐색한다.

- 한 파일 또는 클래스가 지나치게 많은 책임을 가지고 있는 부분
- UI 코드와 계산 로직이 직접 결합된 부분
- 동일하거나 유사한 계산이 여러 위치에 구현된 부분
- 조건문이 과도하게 중첩된 부분
- 문자열/상수/ID에 의존하는 분기
- 전역 상태 또는 암묵적인 상태 변경
- 함수 호출 과정에서 데이터가 불필요하게 변환되는 부분
- 순환 의존성
- 사용되지 않는 코드
- 임시 패치가 누적된 코드
- 테스트하기 어려운 구조

---

## 3. 리팩토링 계획 수립

분석 후 바로 전체 코드를 한 번에 변경하지 않는다.

먼저 다음 형식으로 리팩토링 계획을 제시한다.

```text
[Refactoring Plan]

Phase 1 — 안전한 정리
- dead code 제거
- 이름 개선
- 중복 제거
- 상수 정리

Phase 2 — 책임 분리
- UI / 계산 / 데이터 로직 분리
- 과도하게 큰 함수 및 클래스 분할

Phase 3 — 데이터 흐름 개선
- 상태 관리 정리
- 입력 → 계산 → 결과 흐름 단순화

Phase 4 — 계산 엔진 구조 개선
- 공통 계산 로직 통합
- 조건부 효과 처리 구조 정리
- 무기/스킬/장비별 특수 처리 분리

Phase 5 — 검증
- 기존 테스트
- 회귀 테스트
- 주요 계산 결과 비교
```

각 Phase마다 다음을 설명한다.

- 현재 문제
- 변경 대상
- 변경 이유
- 예상 영향 범위
- 회귀 위험

---

## 4. 변경 원칙

### 기능 보존

기존 계산 결과를 임의로 변경하지 않는다.

계산식 자체에 문제가 있다고 판단되는 경우에도 리팩토링과 버그 수정은 분리한다.

```text
Refactoring
≠
Behavior Change
```

버그를 발견했다면 먼저 보고하고, 리팩토링 과정에서 몰래 수정하지 않는다.

### 최소 단위 변경

가능하면 작은 단위로 변경한다.

한 번의 변경에서 서로 관계없는 영역을 동시에 수정하지 않는다.

### 명확한 책임

하나의 함수/클래스/모듈이 지나치게 많은 일을 하지 않도록 한다.

특히 다음 구조를 목표로 한다.

```text
UI
 ↓
Application
 ↓
Calculation / Domain
 ↓
Data
```

하위 계층이 UI에 의존하는 역방향 구조는 피한다.

### 계산 규칙의 중앙화

같은 개념의 계산 규칙이 여러 파일에 흩어져 있다면 가능한 한 하나의 책임 영역으로 통합한다.

예:

```text
근거리 위력 판정
원거리 위력 판정
무기 타입 판정
스킬 거리 판정
조건부 스탯 적용
장비 옵션 적용
```

동일한 판정 규칙을 UI, 필터, 계산 엔진 등이 각자 다시 구현하지 않도록 한다.

---

## 5. 테스트 및 회귀 검증

각 주요 리팩토링 단계가 끝날 때마다 테스트를 실행한다.

기존 회귀 테스트가 존재한다면 반드시 유지한다.

특히 과거 버그를 검증하는 테스트는 삭제하거나 완화하지 않는다.

기존 테스트가 부족하다면 구조를 크게 변경하기 전에 주요 동작을 고정하는 characterization/regression test를 추가한다.

검증 대상에는 최소한 다음을 포함한다.

- 동일 입력에 대한 최종 계산 결과
- 장비 옵션 적용
- 크리스타 옵션 적용
- 스킬 효과 적용
- 조건부 효과
- 무기 종류별 분기
- 근거리/원거리 판정
- UI 입력 → 계산 → 결과 출력 흐름

테스트 실패를 해결하기 위해 기대값을 임의로 수정하지 않는다.

---

## 6. 불필요한 리팩토링 금지

다음은 명확한 이점이 없다면 하지 않는다.

- 단순히 최신 문법으로 바꾸기
- 필요 이상의 추상화
- 과도한 디자인 패턴 도입
- 모든 코드를 클래스화
- 작은 함수를 의미 없이 여러 단계로 분할
- 기존에 명확한 코드를 단순히 스타일 취향 때문에 변경
- 프로젝트 전체 이름을 대규모로 변경

리팩토링의 기준은 **코드가 달라졌는가**가 아니라 **구조가 실제로 좋아졌는가**이다.

---

## 7. 작업 결과 보고

각 단계가 끝나면 다음 형식으로 보고한다.

```text
[Refactoring Result]

변경:
- ...

구조 개선:
- ...

제거한 중복:
- ...

영향 범위:
- ...

테스트:
- PASS / FAIL

기존 동작 변경:
- 없음
```

기존 동작이 변경되었다면 반드시 별도로 표시한다.

전체 리팩토링이 끝나면 최종적으로

```text
Before
→ 문제점

After
→ 개선된 구조

Remaining Issues
→ 아직 남아 있는 기술 부채

Recommended Next Step
→ 이후 개선 우선순위
```

를 정리한다.